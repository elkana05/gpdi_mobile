name: gpdi_mobile
description: "Base Project Aplikasi Jemaat GPdI Sibulele"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  dio: ^5.4.3
  flutter_secure_storage: ^9.2.1
  go_router: ^14.0.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Nantinya Anda bisa mendaftarkan assets gambar di sini
  # assets:
  #   - assets/images/